---
all:
  children:
    physical_hosts:
      hosts:
        alexandria:
        andromeda:
        enterprise:
    raspbian_nodes:
      hosts:
        andromeda:
    virtual_machines:
      children:
        qemu_vms:
          hosts:
            norville:
            codsworth:
            memory-alpha:
        lxc_containers:
          hosts:
            centaurus:
    proxmox_nodes:
      hosts:
        enterprise:
    nas_nodes:
      hosts:
        alexandria:
    hosts_with_proxied_services:
      hosts:
        alexandria:
        andromeda:
        codsworth:
        enterprise:
        memory-alpha:
        norville:
    pihole_nodes:
      hosts:
        # Order matters — proxmox_dns_servers derives resolver order from this list.
        # centaurus is primary, andromeda is secondary.
        centaurus:
        andromeda:
    self_managed_os:
      children:
        proxmox_nodes:
        nas_nodes:
      hosts:
        # haos
        codsworth:
        # gitlab
        memory-alpha:
