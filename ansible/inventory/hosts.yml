---
all:
  children:
    physical_hosts:
      hosts:
        enterprise:
        andromeda:
    raspbian_nodes:
      hosts:
        andromeda:
    virtual_machines:
      children:
        qemu_vms:
          hosts:
            centaurus:
            norville:
            dorothy:
            memory-alpha:
            hermes:
    proxmox_nodes:
      hosts:
        enterprise:
    nas_nodes:
      hosts:
        alexandria:
    pihole_nodes:
      hosts:
        # Order matters — proxmox_dns_servers derives resolver order from this list.
        # centaurus is primary, andromeda is secondary.
        centaurus:
        andromeda:
    self_managed_os:
      children:
        proxmox_nodes:
        nas_nodes:
      hosts:
        # haos
        codsworth:
        # gitlab
        memory-alpha:
        # hermes, maybe
    apt_managed:
      hosts:
        andromeda:
        centaurus:
        dorothy:
        norville:
