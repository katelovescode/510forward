---
all:
  children:
    physical_hosts:
      hosts:
        enterprise:
        andromeda:
    raspbian_nodes:
      hosts:
        andromeda:
        # rpi5_inactive:
        # imac_inactive:

    virtual_machines:
      children:
        lxc_containers:
          hosts:
        # dorothy: # future — Homepage dashboard LXC, not yet implemented
        qemu_vms:
          hosts:
            centaurus:
            norville:
    proxmox_nodes:
      hosts:
        enterprise:
    pihole_nodes:
      hosts:
        # Order matters — proxmox_dns_servers derives resolver order from this list.
        # centaurus is primary, andromeda is secondary.
        centaurus:
        andromeda:
