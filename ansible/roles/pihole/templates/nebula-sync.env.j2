# Managed by Ansible — do not edit manually
PRIMARY=http://{{ hostvars['centaurus'].ip_address }}|{{ lookup('pipe', 'op read "op://' + onepassword_vault + '/Pi-hole Admin Password (centaurus)/password"') }}
REPLICAS=http://{{ hostvars['andromeda'].ip_address }}|{{ lookup('pipe', 'op read "op://' + onepassword_vault + '/Pi-hole Admin Password (andromeda)/password"') }}
FULL_SYNC=true
RUN_GRAVITY={{ nebula_sync_run_gravity | default(false) | lower }}