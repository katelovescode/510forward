# Managed by Ansible — do not edit manually
PRIMARY="http://{{ ip_address }}|{{ pihole_nebula_sync_admin_password }}"
REPLICAS="{% for host in groups['pihole_nodes'] | reject('equalto', inventory_hostname) | list %}http://{{ hostvars[host].ip_address }}|{{ pihole_nebula_sync_admin_password }}{% if not loop.last %},{% endif %}{% endfor %}"
FULL_SYNC=true
RUN_GRAVITY={{ nebula_sync_run_gravity | default(false) | lower }}
