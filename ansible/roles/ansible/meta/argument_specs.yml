---
argument_specs:
  main:
    short_description: Create ansible user and harden SSH
    description: >
      Creates the ansible system user with passwordless sudo and an SSH key
      managed in 1Password. On non-Proxmox hosts, also locks the root account
      and disables root SSH login.
    options: {}
