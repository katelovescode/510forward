repos:
  - repo: local
    hooks:
      - id: check-secrets
        name: Check secrets are encrypted
        entry: python ansible/scripts/check_secrets.py
        language: python
        pass_filenames: false
        files: ^ansible/
        additional_dependencies:
          - PyYAML
