# Terraform/OpenTofu

Provides configuration for homelab infrastructure as code

To run: `tofu plan -var-file=environments/proxmox.production.auto.tfvars`

Use `proxmox.development.auto.tfvars` when testing against a non-production environment
